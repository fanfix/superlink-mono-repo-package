/**
 * Re-export for embed preview (EmbedMaker + getEmbedPlatform).
 * Use this from MobilePreview/components to avoid deep relative paths to src.
 */
export { EmbedMaker } from '../../../components/EmbedMaker';
export { getEmbedPlatform } from '../../../utils/embedHelpers';
