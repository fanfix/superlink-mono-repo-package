// Re-export from single source file (colored + mono icons + URL detection).
export { getSocialIcon, getSocialIconColored, getSocialIconMono, getPlatformFromUrl, SOCIAL_BRAND_COLORS } from './socialIcons';
